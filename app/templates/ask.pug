extends base.pug

block content
    .qa
        header.qa-header
            h3.qa-title= 'Previous questions and answers'

        .qa-content
            each item in data
                +answer(item, moment)