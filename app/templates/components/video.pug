

mixin video(isYoutube, url)
    if isYoutube
        iframe(width=640 height=360 src=url frameborder=0 allowfullscreen=true)
    else
        video(width=640 height=360 controls=false)
            source(src=url type='video/mp4')